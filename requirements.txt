# Core
python-dotenv>=1.0.0

# Data retrieval
requests>=2.31.0

# Data management
pandas>=2.0.0
numpy>=1.25.0

# Schema validation
pydantic>=2.0.0

# LLM + Embedding API
openai>=1.30.0

# Audit logging
portalocker>=3.2.0

# Bayesian modelling
#   Module 3 fits the random-effects model by exact 1-D numerical
#   marginalisation, not MCMC — PyMC / PyTensor are NOT required (and the
#   compiled PyTensor backend is blocked by this machine's security policy).
#   arviz is used only as the InferenceData container.
arviz>=0.18.0

# Power calculations / Wilson intervals
statsmodels>=0.14.0
scipy>=1.11.0

# Scorecard metrics + calibration
scikit-learn>=1.4.0

# Figures (make_poster_figures.py)
matplotlib>=3.8.0

# Dashboard
plotly>=5.20.0
shiny>=1.5.1
shinywidgets>=0.8.1

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Development
ruff>=0.4.0
